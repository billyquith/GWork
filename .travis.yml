language: cpp
os: 
  - osx
  - linux
compiler: clang
env:
  - BUILD_CONFIG=Debug
osx_image: xcode7.3

sudo: required
dist: trusty
      
install: source ./ci/travis/install.sh
  
before_script: source ./ci/travis/make_build.sh
    
script:
  - echo "Target:$CMAKE_TARGET, Config:$BUILD_CONFIG"
  - cmake --build . --target $CMAKE_TARGET --config $BUILD_CONFIG -V

notifications:
  email:
    on_failure: change
    on_success: change
